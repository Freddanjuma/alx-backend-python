apiVersion: v1
kind: Service
metadata:
  name: messaging-app-service
spec:
  selector:
    app: messaging-app
    version: blue  # Currently pointing to Blue. Change to 'green' to switch traffic.
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: ClusterIP
